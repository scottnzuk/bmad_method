**/.DS_Store
**/__pycache__/
*.py[cod]

# Agent Zero project state — local only, never commit
.a0proj/

# Plugin disabled state — keep enabled (.toggle-1) committed, ignore disabled marker
.toggle-0

# Plugin toggle state — local only
.toggle-*
